//! vulpini-rules: parsing and matching for v2ray-format geosite.dat / geoip.dat.
//!
//! Leaf crate: sync parsing only, no tokio, no dependency on vulpini-core.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {}
}
